import { Command } from "../types/Command"

export default <Command>{
  name: "plan",
  description: "Show changes required by the current configuration",
  handler: args => console.log("TODO: Plan")
}
